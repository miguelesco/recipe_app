<h2>Shopping List</h2>
<p>Amount of food items to buy: <span><%= @foods.length %></span> Total value of food needed: <span>$<%= @total_price_all %></span></p>

<table class="table table-success table-striped table-hover" style="max-width: 700px">
  <thead>
    <tr>
      <th>Food</th>
      <th>Quantity</th>
      <th>Price</th>
    </tr>
  </thead>
  <tbody>
    <% @foods.each do |food| %>      food
      <%= render 'recipe_food_row', food: food %>
    <% end %>
  </tbody>
</table>